machine:
  python:
    version: 3.6.1

dependencies:
  override:
    -echo "no installs needed"

test:
  override:
    - echo "no test needed"

deployment:
  all:
    branch: /*.*/
    commands:
      - scripts/deploy-to-qa-server.sh
