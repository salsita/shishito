machine:
  python:
    version: pypy-2.2.1

dependencies:
  override:
    -echo "no installs needed"

test:
  override:
    - echo "no test needed"

deployment:
  all:
    branch: /*.*/
    commands:
      - scripts/deploy-to-qa-server.sh
